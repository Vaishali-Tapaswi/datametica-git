Lab 3 - Datametica-asgns 
    modify project/any files
    commit logs
    Create a file "firstnameLastnameDay1.txt" /.doc 
      Whatever theory and commands which we discussed
      https://git-scm.com/videos
      https://git-scm.com/book/en/v2/
        1. Getting Started (must)
        2. Git Basics
--------
Lab 2 - local repository
  create a new folder(datatmp)
    cmd>dir (hidden folder -> options)
    cmd>git init 
      a folder will be create (dir)
    cmd>git status
    create two text files(aa.txt, bb.txt) (editor)
    cmd>git add aa.txt
    cmd>git status / git show / git log
    cmd>git commit -m message
    cmd>git status / git show / git log
   -------> modify the file (aa.txt and repeat operations to see those changes
   -------> git add bb.txt , commit, observe
----------------
Lab 1 - 
  https://github.com/join
  Create a new github login (if not available)
  
  Create a new repository "tmp"
    public
    Initialize this repository with: readme
   
  create demo.txt
    add one line -> commit (with comment)
    add one more line -> commit
    delete a line -> commit
  demo.txt
    check history
    
